"use client";
import { useState } from "react";
const fields = ["Name", "Email", "Phone optional", "Trip destination", "Travel dates", "Budget", "Family size", "Main concern", "Notes"];
export function ConciergeForm() { const [sent, setSent] = useState(false); const [form, setForm] = useState<Record<string, string>>({ military: "yes" }); return <form className="card p-6" onSubmit={(e) => { e.preventDefault(); setSent(true); }}><div className="grid gap-5 md:grid-cols-2">{fields.map((f) => <label className="grid gap-2" key={f}><span className="label">{f}</span><input className="input" value={form[f] ?? ""} onChange={(e) => setForm({ ...form, [f]: e.target.value })} placeholder={f} /></label>)}<label className="grid gap-2"><span className="label">Military family</span><select className="input" value={form.military} onChange={(e) => setForm({ ...form, military: e.target.value })}><option>yes</option><option>no</option></select></label></div><button className="btn-primary mt-6" type="submit">Request $149 Concierge Review</button>{sent && <p className="mt-4 rounded-2xl bg-mission/10 p-4 font-semibold text-mission">Request captured locally for MVP. Future version will send to Supabase and hosted checkout.</p>}</form>; }
