import { SectionHeader } from "@/components/SectionHeader";
import { adminStats } from "@/lib/mock-data";
export default function AdminPage() { const stats = [["Total trip requests", adminStats.totalTripRequests], ["Family trip requests", adminStats.familyTripRequests], ["Military Leave Mode requests", adminStats.leaveModeRequests], ["Average budget", adminStats.averageBudget], ["Saved trips", adminStats.savedTrips], ["Concierge requests", adminStats.conciergeRequests], ["Affiliate click placeholders", adminStats.affiliateClicks], ["Payment status placeholders", adminStats.paymentStatuses.join(", ")]]; return <section className="section"><SectionHeader eyebrow="Admin Dashboard" title="Mock analytics for MVP operations" description="Production versions should protect this route with authentication and role-based access." /><div className="grid gap-5 md:grid-cols-4">{stats.map(([k, v]) => <div className="card p-5" key={k}><p className="text-sm font-semibold text-slate-500">{k}</p><p className="mt-2 text-2xl font-black text-navy">{v}</p></div>)}</div><div className="card mt-8 p-6"><h3 className="text-xl font-black text-navy">Top destinations</h3><div className="mt-4 flex flex-wrap gap-2">{adminStats.topDestinations.map((d) => <span className="badge bg-mission/10 text-mission" key={d}>{d}</span>)}</div></div></section>; }
