import { SectionHeader } from "@/components/SectionHeader";
import { TripCard } from "@/components/TripCard";
import { tripOptions } from "@/lib/mock-data";
export default function SavedTripsPage() { return <section className="section"><SectionHeader eyebrow="Saved Trips" title="Saved family travel profile and trip plans" description="Authentication is a placeholder for MVP, but the profile UI mirrors the Supabase-ready data model." /><div className="card mb-8 p-6"><h3 className="text-2xl font-black text-navy">Saved Family Travel Profile</h3><div className="mt-5 grid gap-3 text-sm md:grid-cols-3">{["Home city or duty station: Fort Stewart", "Nearest airport: SAV", "Family size: 4", "Kids’ ages: 6, 10", "Pets: 1 dog", "Preferred hotel type: suite", "Driving limit: 6 hours", "Monthly travel budget: $450", "Need kitchen/laundry: yes", "Need refundable bookings: yes", "Military discount preference: yes"].map((x) => <div className="rounded-2xl bg-slate-50 p-3" key={x}>{x}</div>)}</div></div><div className="space-y-6">{tripOptions.slice(0, 2).map((trip) => <TripCard trip={trip} compact key={trip.id} />)}</div></section>; }
