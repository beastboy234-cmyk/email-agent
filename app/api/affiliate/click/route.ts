import { NextResponse } from "next/server";
export async function GET() { return NextResponse.json({ status: "affiliate_click_placeholder", note: "Future redirect tracking for Expedia Rapid, Amadeus, Travelpayouts, Booking.com, or Duffel partners." }); }
