import { NextResponse } from "next/server";
export async function POST() { return NextResponse.json({ status: "concierge_request_placeholder", note: "Future Supabase insert and Stripe/PayPal hosted checkout link." }); }
